import styled from "styled-components";

export const MainWrap = styled.main`
  padding: 4rem 0;
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-wrap: wrap;
  max-width: 800px;
  margin: 0 auto;
`;
